export function DistribusiLevel({ data }: { data: Array<{ label: string; value: number; color: string }> }) { const total = data.reduce((sum, item) => sum + item.value, 0); return <div className="space-y-3">{data.map((item) => <div className="flex items-center gap-3" key={item.label}><span className="h-3 w-3 rounded-full" style={{ background: item.color }} /><span className="flex-1 font-semibold">{item.label}</span><span>{item.value} siswa</span><strong style={{ color: item.color }}>{Math.round(item.value / total * 100)}%</strong></div>)}</div>; }
