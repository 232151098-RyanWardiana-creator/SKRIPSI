export function ProgressBar({ value, label }: { value: number; label?: string }) { return <div>{label && <div className="mb-1 flex justify-between text-xs"><span>{label}</span><span>{value}%</span></div>}<div className="h-2 overflow-hidden rounded-full bg-[#e6e8f1]"><div className="h-full rounded-full bg-[#0066cc] transition-all" style={{ width: `${Math.min(100, value)}%` }} /></div></div>; }
