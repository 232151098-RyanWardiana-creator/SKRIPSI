export function Stepper({ steps, active }: { steps: string[]; active: number }) { return <ol className="flex max-w-full items-center gap-2 overflow-x-auto pb-1">{steps.map((step, index) => <li className={`flex shrink-0 items-center gap-2 rounded-full px-2 py-2 text-sm md:px-3 ${index === active ? "bg-white font-semibold text-[#1d1d1f]" : index < active ? "text-[#0066cc]" : "hidden text-[#7a7a7a] md:flex"}`} key={step}><span className={`grid h-6 w-6 place-items-center rounded-full text-xs ${index <= active ? "bg-[#0066cc] text-white" : "bg-[#e1e2eb]"}`}>{index + 1}</span><span className={index === active ? "inline" : "hidden md:inline"}>{step}</span></li>)}</ol>; }
